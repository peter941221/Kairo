# Kairo publication drafts

`001-when-cuda-graphs-actually-help.md` is the Phase 1 launch article. It is
the canonical narrative: reproducible benchmark, measured CUDA-Graph boundary,
fail-closed routing, and the start of native Blackwell experiments.

`002` and `003` are retained as follow-up source drafts. They should not be
published as separate launch posts until their public evidence bundles exist.

Only Kairo publication materials belong in this directory. Private editorial
references must remain untracked.
